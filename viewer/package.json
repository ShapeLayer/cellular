{
  "name": "cellular-viewer",
  "version": "0.1.0",
  "description": "Cellular viewer - static heatmap frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 8080 --host 127.0.0.1 --strictPort"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.2.1"
  },
  "dependencies": {
    "fflate": "^0.8.3"
  }
}
